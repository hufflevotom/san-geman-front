import FusePageCarded from '@fuse/core/FusePageCarded';
import withReducer from 'app/store/withReducer';
import { styled } from '@mui/material/styles';
import reducer from '../store';
import ControlFacturasHeader from './controlFacturasHeader';
import ControlFacturasTable from './controlFacturasTable';

const Root = styled(FusePageCarded)(({ theme }) => ({
	'& .FusePageCarded-header': {
		minHeight: 72,
		height: 72,
		[theme.breakpoints.up('sm')]: {
			alignItems: 'center',
			minHeight: 136,
			height: 136,
		},
	},
	'& .FusePageCarded-content': {
		display: 'flex',
	},
	'& .FusePageCarded-contentCard': {
		overflow: 'hidden',
	},
}));

function ControlFacturas() {
	return <Root header={<ControlFacturasHeader />} content={<ControlFacturasTable />} innerScroll />;
}

export default withReducer('logistica', reducer)(ControlFacturas);
